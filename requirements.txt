Flask==2.3.3
Werkzeug>=3.0,<3.1
ffmpeg-python==0.2.0
numpy==1.24.3
scipy==1.11.1
Pillow==10.0.0
pillow-heif==0.13.0
opencv-python==4.8.0.76
gunicorn==21.2.0
requests==2.31.0
# PDF handling — pdfplumber (built on pdfminer.six) for the text+table
# layer, pdf2image for per-page rasterization (shells out to poppler), pypdf
# for cheap page-count probing without a full parse. See `/pdf-to-text` and
# `/pdf-to-pages`. We use pdfplumber directly rather than Microsoft's
# markitdown because markitdown requires Python 3.10+ and the existing
# ffmpeg/scipy/opencv stack is pinned to python:3.9-slim — pdfplumber is
# exactly what markitdown calls into for PDFs anyway, so the output is the
# same minus markitdown's own thin wrapper. DOCX/DOC/RTF/ODT keep going
# through libreoffice (`/docx-to-text`), which is already proven.
pdfplumber>=0.11.0
pdf2image>=1.17.0
pypdf>=4.0.0