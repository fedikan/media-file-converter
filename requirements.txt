Flask==2.3.3
Werkzeug>=3.0,<3.1
ffmpeg-python==0.2.0
numpy==1.24.3
scipy==1.11.1
Pillow==10.0.0
pillow-heif==0.13.0
opencv-python==4.8.0.76
gunicorn==21.2.0
requests==2.31.0
# Document → markdown text. Microsoft's markitdown handles PDF, DOCX, DOC,
# PPTX, XLSX, HTML, EPUB, CSV, JSON, XML, TXT, .msg, ZIP archives and more
# in one library. Requires Python ≥3.10 — we bumped the base image to
# python:3.11-slim. The `[all]` extra installs every format handler. No LLM
# client is wired in, so image/audio modes (which require an API key) are
# silently skipped — callers should keep using `/pdf-to-pages` + image-
# specific endpoints for those.
markitdown[all]>=0.1.0
# pdf2image is kept ONLY for `/pdf-to-pages` per-page raster output. Markitdown
# does not rasterize PDFs (it converts the text layer to markdown only).
# Shells out to poppler-utils (installed in the Dockerfile).
pdf2image>=1.17.0
